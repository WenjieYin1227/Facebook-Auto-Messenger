# facebook_auto_messanger
Using pip, you can install selenium like this:
pip install selenium

Using pip, you can install bs4 like this:
pip install bs4

Using pip, you can install emoji like this:
pip install emoji